# 任务清单：新增「近期行程」视图（补记 + 修正默认视图）

> 实现请配合 `/opsx:apply`，按顺序逐项完成并勾选。

## 1. 修正与 family-schedule 规范冲突的默认视图
- [x] 1.1 `src/App.tsx` 默认视图 `'trip'` → `'family'`，使实现符合「默认首页」需求
- [x] 1.2 导航顺序保持「近期行程 → 家庭日程 → 房产看板」不变
- [x] 1.3 确认三个视图仍保持挂载（`hidden` 切换显隐），切换视图不重置各自筛选状态

## 2. 规范补记
- [x] 2.1 新增 `specs/trip-schedule/spec.md`：数据模型、概览、阶段与倒计时、逐日行程、事件展示、类型筛选、预订状态、出发前任务、多行程切换、家庭日程提示、数据来源
- [x] 2.2 新增 `specs/family-schedule/spec.md`：MODIFIED 视图切换（三视图 + 导航顺序 + 不重置筛选）与默认首页（默认家庭日程，即使近期行程居首）

## 3. 验证
- [x] 3.1 `npm run build` 通过（tsc + vite）
- [ ] 3.2 本地 `npm run dev` 手测：打开根路径默认进入家庭日程、三视图切换、家庭日程顶部行程提示条显示/隐藏、行程类型筛选

## 4. 部署与归档
- [x] 4.1 `git commit` 代码修正与规范产物
- [x] 4.2 部署前端（注意 `--exclude data`）
- [x] 4.3 归档变更并把两份 delta 同步到 `openspec/specs/`
